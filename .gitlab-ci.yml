stages:
  - "linter"
  - "testing"
  - "build"
  - "deploy"

pint:
  stage: "linter"
  script:
    - echo "pint 檢查"
    - chmod +x ./pint.sh
    - ./pint.sh

bad_job:
  stage: "testing"
  script:
    - echo "Bad Job"
    - dfodjfodjfiodjfodfjao

run_tests:
  stage: "testing"
  needs:
    - bad_job
  script:
    - echo "執行 testing 檢查"

build_image:
  stage: "build"
  script:
    - echo "打包 docker image"

deploy:
  stage: "deploy"
  script:
    - echo "部署至主機"